server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://192.168.126.131:3306/lost_found?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: djx55707
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: 192.168.126.131
      port: 6379

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 30MB

file:
  upload-dir: uploads

backup:
  output-dir: backups/sql

springdoc:
  swagger-ui:
    path: /swagger-ui.html
